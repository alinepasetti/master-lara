import { Meta, Story } from '@storybook/react/types-6-0';
import { PantrySection } from '.';

export default {
  title: 'PantrySection',
  component: PantrySection,
} as Meta;

export const Template: Story = () => <PantrySection />;
